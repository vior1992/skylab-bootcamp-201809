const root = document.getElementById('root')

class plusButton extends React.Component {

    whenClicked = (num1, num2) => {
        return 
    }

    render() {
        return <button onClick={this.whenClicked}>{this.state.status}</button>
    }
}

ReactDOM.render(<App />, root)


